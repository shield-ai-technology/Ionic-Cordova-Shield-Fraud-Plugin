def shieldFraudMavenUrl = "https://cashshield-sdk.s3.amazonaws.com/release/"

rootProject.allprojects {
  repositories {
    maven {
      url shieldFraudMavenUrl
    }
  }
}

repositories {
  maven {
    url shieldFraudMavenUrl
  }
}

dependencies {
  implementation("com.shield.android:shield-fraud:2.4.0")
}
